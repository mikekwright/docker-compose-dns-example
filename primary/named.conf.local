logging {
    channel debug_query {
        stderr;
        severity dynamic;
    };

    category queries { debug_query; };
};

zone "resero-dns.com" {
    type master;
    file "/etc/bind/zones/db.resero-dns.com";
    allow-transfer { 172.20.0.3; };
};

